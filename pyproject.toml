[project]
name = "qums-fetch"
version = "0.1.0"
description = "Login and fetch data from QUMS-ERP Dashboard"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
